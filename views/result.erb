<div class="container">
  <div class="gacha-container">
    <h2><%= @selected_item[:text] %></h2>
    
    <!-- 結果表示 -->
    <div class="result-image">
      <img src="/images/<%= @selected_item[:image] %>" alt="<%= @selected_item[:text] %>">
    </div>

    <!-- もう一度ボタン -->
    <div class="action-buttons">
      <button class="gacha-button" onclick="location.href='/'">
        もう一度ハイキング！
      </button>
    </div>
  </div>
</div>